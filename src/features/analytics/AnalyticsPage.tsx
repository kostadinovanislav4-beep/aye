import { MODULE } from '../../app/modules'
import { PlaceholderPage } from '../../components/PlaceholderPage'

export default function AnalyticsPage() {
  return <PlaceholderPage module={MODULE.analytics} />
}
