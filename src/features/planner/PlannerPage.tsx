import { MODULE } from '../../app/modules'
import { PlaceholderPage } from '../../components/PlaceholderPage'

export default function PlannerPage() {
  return <PlaceholderPage module={MODULE.planner} />
}
