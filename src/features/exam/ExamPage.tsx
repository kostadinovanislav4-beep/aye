import { MODULE } from '../../app/modules'
import { PlaceholderPage } from '../../components/PlaceholderPage'

export default function ExamPage() {
  return <PlaceholderPage module={MODULE.exam} />
}
