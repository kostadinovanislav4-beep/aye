import { MODULE } from '../../app/modules'
import { PlaceholderPage } from '../../components/PlaceholderPage'

export default function ModesPage() {
  return <PlaceholderPage module={MODULE.modes} />
}
